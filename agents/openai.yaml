interface:
  display_name: "PM Meeting Notes"
  short_description: "把会议转写整理成纪要、HTML、文档和表格"
  default_prompt: "Use $pm-meeting-notes to turn this meeting transcript into structured meeting minutes and export a shareable HTML or document artifact."
policy:
  allow_implicit_invocation: true
